# UK Wave 3 build: shared agent brief (2026-09-24)

Every builder agent reads this whole file first. It is the contract. Your page-specific task comes in your prompt.

## Repo and hard safety rules
- Repo: /Users/bhaveshbarot/FactoryJet/website (Next.js 15, React 19, TS, Tailwind 4). Branch NextJS. Other sessions work in this tree at the same time.
- **NEVER run `next dev`, `next build`, `npm run dev/build`, or any local render.** The machine runs out of memory. Do NOT run a full `npx tsc --noEmit` either (it stalls for minutes and the harness kills you). The orchestrator runs all gates.
- Create/edit ONLY the files named in your prompt, plus your own images folder. Do NOT touch: sitemaps, footers, headers, `posts.tsx`, `llms.txt`, `_redirects`, hreflang maps, other pages, `src/data/organization.ts`. The orchestrator wires links centrally.
- Do NOT git add, commit, or push.
- Never print API keys. DataForSEO creds: `pipeline/research/.env`. Runware key: `RUNWARE_API_KEY` in `/Users/bhaveshbarot/FactoryJet/website/.env.local`.

## Research you already have (no new DataForSEO spend needed)
- Your brief JSON: `pipeline/research/data/market-uk-2026-09-24/brief_<page>.json` = `{keywords:[{keyword,vol,cpc,kd,intent,question,buyer}], serps:[{keyword,top10,titles,aio,aio_refs,paa}]}`. UK volumes (location GB), fetched today.
- Market context: `pipeline/research/UK-MARKET-RESEARCH-2026-09-24.md` (read section on your topic + section 7). LLM answers for UK buyer prompts: `pipeline/research/data/market-uk-2026-09-24/llm/` and `llm_calls.json` (grep for your topic).
- **Ignore navigational brand noise** in keyword lists (e.g. "nissan b2b portal", "cormar trade portal", "easyjet trade portal", "emma ai gp receptionist", "ringcentral ai receptionist"). Those are people looking for someone else's login or product.
- Treat close variants as one cluster (e.g. "ecommerce consultant" / "consultant ecommerce" / "ecommerce consulting" are one 590 cluster, not 1,770).

## Design: mirror the reference page exactly
- Service pages: mirror `src/app/uk/ai-consulting/page.tsx` (built today): same imports (`HeroInlineForm`, `../sections/Footer`, `Breadcrumbs`, `ModalCTAButton`, `MidPageCTA`, `../ai-agents/ai-agents.css`), same section classes, same metadata shape (`alternates: { canonical, languages: { 'en-GB': CANONICAL, 'x-default': CANONICAL } }`, `locale: 'en_GB'`), same JSON-LD approach. Read it fully before writing. Also skim `src/app/uk/magento/page.tsx`. Do not invent component props: copy usage exactly from the reference.
- Design rules: cream background, light hero (never dark), Plus Jakarta Sans display / Inter body / Geist Mono eyebrows via the existing classes, left-aligned asymmetric layouts, at most ONE dark section, no gradient text, no glow, no particles, no count-up numbers. Small orange text uses #B23E13, never white text on #F05A28 below ~18px.
- Plain `<img>` with width/height, hero `fetchPriority="high"`, everything below the fold `loading="lazy"`.

## Content gate (every item mandatory)
1. **Answer-first block** right after the hero: the page's #1 question answered in 40 to 60 extractable words.
2. **Keywords:** 15 to 30 real terms from your brief JSON woven naturally into H1, title, meta description, H2s, body, FAQ. Primary term in title, H1, first 100 words, one H2, meta description. Never stuffed.
3. **Meta title** 50 to 60 characters, ends with "| FactoryJet". **Meta description** 140 to 160 characters, includes primary term + UK + a reason to click.
4. **20 to 30 FAQs** written the way a real UK buyer asks, grounded in the PAA questions in your brief (`serps[].paa`). Answers direct, 40 to 100 words, no hedging. Exclude career/salary/course questions. **FAQPage JSON-LD must be generated by mapping the SAME array the visible FAQ renders.** Never a second hand-written list.
5. **At least one listicle** (numbered "N things" / steps) and **one comparison table** (e.g. options compared, or in-house vs freelancer vs agency).
6. **Named UK competitors** from your brief's `top10` / LLM answers, with honest self-disclosure ("we are one of the options"). Verify each on its own homepage before naming (fetch it). Put them in an ItemList schema. Never disparage.
7. **2 to 3 authoritative external citations** (gov.uk, ONS, ICO, Ofcom, NHS England, BCC, industry bodies, platform docs). **Fetch-verify every URL** returns 200 and says what you claim. Do not cite ICO fine totals (JS-rendered, unverifiable). UK wage examples must use the £12.71 National Living Wage if you use one at all (prefer not to).
8. **E-E-A-T:** founder Bhavesh Barot (Founder & CEO), 500+ businesses served (verified), founded 2014, decade-plus in commerce. Do not invent case studies, client names, review counts, percentages or ratings. Named clients allowed: Bombay Petals (B2B), Belle Maison (DTC).
9. **Mid-page CTA** (`MidPageCTA`) plus hero form and a final CTA, same as the reference.
10. **Schema:** WebPage (+ speakable, `dateModified: '2026-09-24'`), Service with `provider: { '@id': 'https://factoryjet.com/#organization' }`, FAQPage (derived), ItemList (competitors), BreadcrumbList. **Never define the Organization** (no name/address/phone/logo/sameAs for FactoryJet). Reference it only by `@id`.
11. **Internal links out** to relevant existing pages (check the route exists under `src/app` first): e.g. `/uk/ai-agents`, `/uk/ai-consulting`, `/uk/ecommerce-development`, `/uk/shopify-development`, `/uk/magento`, `/uk/seo`, `/contact`. Only link to routes that exist today.
12. **Depth:** match `/uk/ai-consulting` (roughly 60 to 66 KB of source, 2,500 to 3,500 words of real value). No filler.

## Writing rules (hard)
- Plain English at a 12-year-old reading level. Explain every jargon term the first time.
- **No em dashes (—) and no "--".** En dashes only in numeric ranges.
- **No currency amounts anywhere** (no £, $, per month figures, day rates). Describe cost drivers and engagement shapes ("fixed-scope build", "monthly support") and route to "Talk to the Founder". No "cheap" or "affordable".
- Banned slop: unlock, elevate, leverage, seamless, robust, supercharge, game-changer, dive in, in today's fast-paced world, it's important to note, cutting-edge, empower, revolutionise.
- **FactoryJet is a services company.** Never "instead of hiring", "replaces your staff", "runs itself", "no more manual work", "without headcount". Say: we design, build, implement and support it, and you own it. The enemy is agencies that disappear after launch.
- Company name is only "FactoryJet". Never "FactoryJet Technologies". No phone numbers anywhere (WhatsApp links are fine).
- British English spelling (optimise, organisation, colour) and UK context (GDPR/UK GDPR, VAT, Companies House, Royal Mail, UK bank holidays) where relevant.

## Images
- 2 or 3 images: hero (1400x933) + og (1200x630 centre crop) + one in-body. Folder `public/images/uk/<page>/`, webp via `cwebp -q 82`, plus `IMAGE-CREDITS.txt` with model and prompts (see `public/images/uk/magento/IMAGE-CREDITS.txt`, reuse its style suffix).
- Generate with Runware `openai:gpt-image@2`, 1536x1024. AI-generated people ARE allowed. Prompt screens with "no words and no numbers on screen". Pin any brand as invented, never real logos. **Open and inspect every image at full size** (Read tool) for hands, faces, garbled text, real brands. Regenerate if any fail.

## Before you finish, self-check and report
- `grep -n "—\|--" <file>` returns nothing in copy; `grep -n "£\|\\$[0-9]"` returns nothing.
- FAQ schema is `.map` over the rendered array.
- Syntax check your file only: `node -e "const ts=require('/Users/bhaveshbarot/FactoryJet/website/node_modules/typescript');const f=process.argv[1];const s=ts.createSourceFile(f,require('fs').readFileSync(f,'utf8'),99,true,4);console.log(s.parseDiagnostics.length?s.parseDiagnostics.map(d=>d.messageText):'OK')" <file>`
- Final report (under 400 words): files created, primary + top 15 keywords used, meta title + description, FAQ count, competitors named, citations with URLs, images and review notes, anything you were unsure about, and the internal links the ORCHESTRATOR should add pointing TO your page.
