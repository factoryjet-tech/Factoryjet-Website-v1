/**
 * Google Ads keyword research provider — STUB.
 *
 * Implementation lands in Patch #11, after Google Ads API Basic Access
 * is approved (application submitted Apr 29, 2026; expected response
 * window May 4–8, 2026).
 *
 * Setup steps for when approval lands:
 *   See pipeline/scripts-ts/docs/google-ads-setup.md
 *
 * Will implement KeywordResearchProvider against:
 *   - KeywordPlanIdeaService.GenerateKeywordIdeas
 *   - KeywordPlanIdeaService.GenerateKeywordHistoricalMetrics
 *
 * No code yet — exporting nothing keeps providers/index.ts honest about
 * what's actually registered until the implementation arrives.
 */

export const GOOGLE_ADS_PROVIDER_PENDING = true as const;
